<p id="notice">
</p>

<!--c,r colmd8で安定しやすかも-->
<div class="container">
<div class="row">
<div class="col-md-8 col-sm-12 px-sm-0 mx-auto">
  
<!--何故かこれで動く、不明https://stackoverflow.com/questions/59523676/ruby-on-rails-encountered-a-syntax-error-while-rendering-template-->
  <h1>Editing Book</h1>
  <div class="errors-text">
      <% if @book.errors.any? %>
    <h3>
      <%= pluralize(@book.errors.count, "error") %> prohibited this product from being saved:
    </h3>
    　<% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    　<% end %>
    　<% end %>
  </div>
  
  
  <%= form_with model:@book, local: true do |f| %>
  
  <div class="form-group">
    <label>Title</label>
    <%= f.text_field :title, class:"form-control" %>
  </div>

  <div class="form-group">
    <label>Opinion</label>
    <%= f.text_area :body, class:"form-control" %>
  </div>

    <%= f.submit "Update Book", class:"btn btn-success" %>
    <% end %>

  <p>
    <%= link_to "Show", book_path(@book) %> |
    <%= link_to "Back", books_path %>
  </p>
      
    </div>
  </div>
</div>

